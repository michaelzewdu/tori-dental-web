name: Deploy CI(Webhook call)

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Call Webhook (Deploy)
        run: |
          curl -X POST "${{ secrets.DEPLOY_WEBHOOK_URL }}"